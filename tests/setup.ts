import { beforeAll, afterAll, beforeEach, afterEach, spyOn } from 'bun:test';

// Global setup
beforeAll(() => {
  // console.log('Global setup...');
});

afterAll(() => {
  // console.log('Global teardown...');
});

// Reset mocks between tests
beforeEach(() => {
  // Restore all spies
});
